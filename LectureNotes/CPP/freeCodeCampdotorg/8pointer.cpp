#include <iostream>

int main() {
    // // A pointer is a variable that stores the memory address of another variable.
    // int value = 10;

    // // Declare a pointer to int and initialize it with the address of 'value'.
    // int *ptr = &value; // '&value' gives the address of value.

    // std::cout << "value: " << value << "\n";
    // std::cout << "address stored in ptr: " << ptr << "\n";
    // std::cout << "value through pointer (*ptr): " << *ptr << "\n";
    // std::cout << "address of value (&value): " << &value << "\n";

    // // Change the original value using the pointer.
    // *ptr = 20; // '*' is the dereference operator.

    // std::cout << "updated value: " << value << "\n";
    // std::cout << "updated value through pointer: " << *ptr << "\n";

    // int value = 10;
    // int *ptr , value2;
    // std::cout << "size of value: " << sizeof(value) << "\n";
    // std::cout << "size of value2: " << sizeof(value2) << "\n";
    // std::cout << "size of ptr: " << sizeof(ptr) << "\n";

    

    return 0;
}
